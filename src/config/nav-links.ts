// Navigation is managed inline by each page/layout.
// The landing page has its own sticky navbar with smooth-scroll anchors.
// The dashboard has its own inline header.
export const navLinks: { label: string; href: string }[] = [];
